# 주문을 실제로 받으려면 — 사장님이 하실 일

주문서와 접수는 이미 동작합니다. 아래 두 가지만 하시면 됩니다.
코드는 건드릴 필요가 없습니다. Vercel 프로젝트의 Environment Variables 에
값을 넣고 재배포하면 그때부터 적용됩니다.

## 0단계 — 판매자 정보 채우기 (필수, 2분)

`build/company.json` 에 아래를 채우고 `python build/build.py` 를 돌리십시오.

    "ceo":            대표자명
    "bizNo":          사업자등록번호
    "mailOrderNo":    통신판매업 신고번호
    "address":        사업장 주소
    "privacyOfficer": 개인정보 보호책임자

「전자상거래법」 제13조가 주문 화면에 요구하는 항목입니다. 저는 없는 번호를
지어낼 수 없어 비워 두었고, **비어 있는 항목은 화면에 아예 나가지 않습니다.**
채우기 전까지 빌드할 때마다 경고가 뜹니다. 실제 주문을 받기 시작하면
이건 선택이 아니라 위반입니다.

## 1단계 — 주문이 어디로 갈지 정하기 (필수, 5분)

지금은 `/api/order` 가 `{"ready":false}` 를 돌려주고, 주문서는 그 사실을
숨기지 않고 "지금은 온라인 접수를 받을 수 없습니다"라고 말합니다.
아래 중 **하나만** 넣으면 바로 살아납니다.

| 변수 | 값 | 결과 |
|---|---|---|
| `LEAD_WEBHOOK_URL` | JSON 을 받는 아무 주소 (Zapier, Make, 구글 시트에 연결한 Apps Script, n8n) | 주문이 그 주소로 도착 |
| `SLACK_WEBHOOK_URL` | 슬랙 수신 웹훅 | 주문이 슬랙 메시지로 도착 |
| `RESEND_API_KEY` + `LEAD_TO_EMAIL` + `LEAD_FROM_EMAIL` | Resend 키와 받을 주소, 보낼 주소 | 주문이 메일로 오고, **구매자에게도 확인 메일이 갑니다** |

가장 추천하는 것은 세 번째입니다. 구매자가 주문번호가 적힌 확인 메일을
받는 것과 못 받는 것의 차이가 큽니다.

주문 전용으로 나누고 싶으시면 `ORDER_WEBHOOK_URL` 과 `ORDER_TO_EMAIL` 을
쓰시면 됩니다. 없으면 위의 `LEAD_*` 를 그대로 씁니다.

여러 개를 넣으면 전부로 갑니다. 하나라도 성공해야 구매자에게 "접수됐다"고
답하고, 전부 실패하면 실패했다고 말합니다. 성공한 척하지 않습니다.

## 2단계 — 카드 결제 붙이기 (나중에 해도 됩니다)

지금 구조는 **주문 접수까지만** 하고, 담당자가 확인 연락을 드린 뒤
결제 안내를 보내는 방식입니다. 화면도 정확히 그렇게 말합니다
("아직 결제되지 않았습니다"). 초기에는 이 편이 오히려 낫습니다.
첫 고객 몇 분은 어차피 통화를 하게 되고, 그 통화에서 배우는 것이 많습니다.

카드 자동결제를 붙일 때가 되면 국내에서는 두 곳 중 하나입니다.

- **포트원(구 아임포트)** — 여러 PG 를 한 번에 붙일 수 있습니다. 정기결제(빌링키) 지원.
- **토스페이먼츠** — 단독. 정기결제 지원.

붙이는 순서:

1. 해당 사에 가맹점 신청을 하고 심사를 받습니다 (사업자등록증, 통장 사본 등).
   **이 단계는 사장님만 하실 수 있습니다.** 저는 계정을 만들거나 인증 정보를
   입력할 수 없습니다.
2. 발급받은 키를 Vercel 환경변수에 넣습니다.
   - `PAYMENT_PROVIDER` = `portone` 또는 `toss`
   - `PORTONE_API_SECRET` 또는 `TOSS_SECRET_KEY`
3. 그 순간부터 `/api/order` 의 응답이 `status: "awaiting_payment"` 로 바뀌고,
   주문서가 결제창을 띄우는 단계를 추가로 보여 주게 됩니다.

카드 번호는 어느 단계에서도 저희 서버를 지나가지 않습니다. 결제사의 창에서
결제사로 바로 갑니다. 그래서 저희가 보관할 카드 정보가 없습니다.

## 확인하는 법

```
curl https://claude.saleringo.com/api/order
```

- `{"ready":false}` → 1단계가 아직입니다. 주문서는 접수를 받지 않습니다.
- `{"ready":true,"confirmation":true,"payment":null}` → 주문을 받습니다.
  구매자에게 확인 메일도 갑니다. 결제사는 아직 없습니다.
- `"payment":"portone"` → 결제까지 붙었습니다.

## 스스로 확인하는 법

```
node build/test_order.mjs
```

74가지 상황을 확인합니다. 금액 위조, 사업자등록번호 오타, 필수 동의 누락,
같은 주문 두 번 누르기, 없는 요금제, 봇, 과도한 요청 등입니다.
요금표를 고치신 뒤에도 이걸 한 번 돌려 보시면 됩니다.


## 사장님이 판단하셔야 할 것 하나

영문 이용약관(`en/terms.html`) 제2조가 아직 **"USD pricing, 50% off for
3 months"** 라고 적고 있습니다. 그런데 지금 영문 주문서는 원화로 청구하고
초기 할인 얘기가 없습니다. 둘 중 하나를 맞춰야 합니다.

저는 주문서 쪽만 고쳤습니다 — 환불 조항을 영문 약관 제3조와 일치시키고,
"이 주문은 위 한국 법인이 원화로 청구하며 부가세 10%가 붙는다"는 문장을
넣었습니다. 약관의 가격 조항 자체를 바꾸는 것은 사업상 결정이라 손대지
않았습니다. 초기 할인을 계속 하실 생각이면 주문서에도 넣어야 하고,
안 하실 생각이면 약관에서 빼야 합니다.
